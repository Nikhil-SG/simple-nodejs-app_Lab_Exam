{
  "name": "simple-nodejs-app_Lab_Exam",
  "version": "1.0.0",
  "description": "A simple Node.js application for the lab exam.",
  "main": "index.js",
  "scripts": {
    "test": "mocha --reporter spec",  // Test script using Mocha (you can replace with your testing framework)
    "lint": "eslint .",               // Linting script for code quality (you must install ESLint)
    "start": "node index.js",         // Start script to run the application
    "review": "npm run lint && npm test", // Code review script to lint and run tests
    "build": "echo 'Building project...'",  // Sample build script (customize as necessary)
    "prettify": "prettier --write '**/*.js'"  // Prettier code formatting (optional)
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.1",      // Add your project dependencies here
    "mongoose": "^5.10.9"      // Example dependency, add others if needed
  },
  "devDependencies": {
    "mocha": "^9.1.3",         // Mocha for testing
    "eslint": "^8.0.0",        // ESLint for linting
    "prettier": "^2.5.1",      // Prettier for code formatting (optional)
    "chai": "^4.3.4"           // Chai for assertions in tests
  },
  "engines": {
    "node": ">=14.0.0"          // Minimum Node.js version required
  }
}
